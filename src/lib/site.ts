// shine-for-good.com redirects to the www subdomain in production (Vercel
// domain config), so www is the canonical URL everywhere — sitemap, robots,
// metadataBase, structured data. Keep this the single source of truth.
export const SITE_URL = 'https://www.shine-for-good.com'
